import ReportView from "./ReportView";

export const metadata = {
  title: "Report",
  description: "A report you built — the blocks you chose, over the scope you chose.",
};

export default function Page() {
  return <ReportView />;
}
