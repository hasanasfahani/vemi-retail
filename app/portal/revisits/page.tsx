import StubView from "@/components/market/StubView";

export const metadata = { title: "Revisit Management" };

export default function Page() {
  return <StubView title="Revisit Management" lead="Flag stores for the next route, then verify." />;
}
